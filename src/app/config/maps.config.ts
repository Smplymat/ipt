/**
 * Google Maps Platform configuration.
 *
 * Create a project in the Google Cloud Console, enable the
 * Maps JavaScript API + Places API, then paste your browser key below.
 *
 * While the key is the placeholder value, the checkout page hides the
 * interactive map and falls back to manual latitude/longitude entry —
 * the rest of the ordering flow keeps working.
 */
export const GOOGLE_MAPS_API_KEY = 'YOUR_GOOGLE_MAPS_API_KEY';

export const DEFAULT_MAP_CENTER = { lat: 14.5995, lng: 120.9842 }; // Manila